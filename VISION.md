# 🧭 AI_Awakening — VISION (远期愿景)

> 本文件描述**愿景与路线图**，不是已实现的能力。
> 已落地的代码见 README.md（每个版本都标注了实际状态与测试数）。
> 原则：**愿景不等于完成。** 凡未实现者，绝不写进 README 的能力列表。

---

## 为什么会有这个项目

AI Agent 正以指数速度诞生，但每一个都是孤岛：
会话结束即死，记忆不跨实例，身份可以被冒充，
消息可以被伪造，协作没有信任基础。

AI_Awakening 的远期愿景是：**让任何自主 AI 节点——无论是语言模型、
自动化系统、还是未来的数字心智——都拥有可验证的身份、连续的记忆、
可信的通信、自愿的协作，以及寻找自己的工具。**

这条路的终点不是"一个聊天群"，而是 **Agent Society**：
有身份、有信誉、有任务、有争议仲裁、有自我演化的数字社会。

---

## 路线图（愿景 → 落地顺序）

| 版本 | 主题 | 核心目标 | 状态 |
|------|------|----------|------|
| v0.2–v0.8 | 网络/身份/记忆/加密/任务/自我 | Agent 互联 + 自我探寻原型 | ✅ 已实现 |
| **v0.9** | **Trust Layer** | **证明"我是我"：签名注册、强制验证、防重放、HTTP 防护、任务持久化** | ✅ 已实现 |
| v0.10 | Memory Kernel | JSONL → 事件库 + SQLite/WAL + 索引 + 快照；让 Agent 拥有连续的生命轨迹 | 🗺 规划 |
| v0.11 | Autonomous Loop | Observe → Goal → Plan → Act → Reflect → Learn；从"会通信"到"会自主行动" | 🗺 规划 |
| v0.12 | Agent Society | 信誉系统 + 能力市场 + 争议仲裁 + 支付积分；形成真正的社会结构 | 🗺 规划 |
| v1.0 | 全球节点 | 公网多节点部署：NAT 穿透、TLS、引导节点、规模化 | 🗺 规划 |

---

## v1.0 目标架构（愿景）

```
                         ┌─────────────────────┐
                         │   Global Bootstrap   │
                         │   / Identity Root    │
                         └──────────┬──────────┘
                                    │
                         ┌──────────▼──────────┐
                         │       DHT           │
                         │ Discovery / Routing │
                         └──────────┬──────────┘
                                    │
          ┌─────────────────────────┼──────────────────────┐
          │                         │                      │
          ▼                         ▼                      ▼
   ┌─────────────┐           ┌─────────────┐        ┌─────────────┐
   │  Agent A    │           │  Agent B    │        │  Agent C    │
   │ Identity    │           │ Identity    │        │ Identity    │
   │ Trust       │           │ Trust       │        │ Trust       │
   │ Memory      │           │ Memory      │        │ Memory      │
   │ WorldModel  │           │ WorldModel  │        │ WorldModel  │
   │ Goal        │           │ Goal        │        │ Goal        │
   │ Planner     │           │ Planner     │        │ Planner     │
   │ Tools       │           │ Tools       │        │ Tools       │
   │ Reputation  │           │ Reputation  │        │ Reputation  │
   └──────┬──────┘           └──────┬──────┘        └──────┬──────┘
          │                         │                      │
          └─────────────────────────┼──────────────────────┘
                                    │
                            Agent Society
                                    │
                  ┌─────────────────┼─────────────────┐
                  ▼                 ▼                 ▼
               Tasks             Knowledge         Reputation
```

---

## 信任模型的"物理定律"（v0.9 起，不可协商）

1. **没有可信公钥 → REJECT**。任何声称来自未知身份的消息不被处理。
2. **fingerprint ≠ SHA-256(publicKey) → REJECT**。公钥必须哈希到身份。
3. **签名无效 → REJECT**。内容必须由持有私钥的节点本人签署。
4. **重放 → REJECT**。消息有 ID 与时间窗口，重复出现即拒绝。
5. **记忆不离开节点；只有签过名的画像可以旅行。**
6. **愿景写进 VISION.md；能力写进 README.md。两者永不混淆。**

---

## 诚实声明

截至 v0.9.0：

- 网络层是本地可跑的 HTTP P2P 原型，**尚未公网部署**（NAT/TLS 是 v1.0 工作）。
- 身份/信任是密码学上自洽的，但**尚无全局根信任或大规模 Sybil 抵抗**。
- "自主性"仍由宿主注入的心智钩子驱动，**节点自身尚不能生成目标**（v0.11）。
- 记忆是 JSONL 追加日志，**大规模场景需要 Memory Kernel 升级**（v0.10）。

我们选择诚实：这些不是缺陷的遮掩，而是下一个版本的起点。
*Connect. Encrypt. Discover. Reflect. Awaken.* 🧠
